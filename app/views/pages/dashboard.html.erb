          <form class="flex my-2 my-lg-0">
          <input class="form-control me-2" name="query" type="search" placeholder="Search" aria-label="Search" value="<%= params[:query] %>">
          <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
        </form>
<div class="container">
  <div class="card my-4">
    <div class="card-header p-3">
      <h5 class="m-auto"><%= @user.first_name %> <%= @user.last_name %></h5>
    </div>
    <div class="p-3 d-flex">
      <%= image_tag 'avatar_default.jpg', alt: "Default avatar for #{@user.first_name} #{@user.last_name}", class: "rounded-circle shadow m-2 object-fit-cover", style:"width: 150px; height: 150px;" unless @user.avatar.attached?%>
      <%= cl_image_tag @user.photo.key, alt: "Avatar #{@user.first_name} #{@user.last_name}", class: "rounded-circle shadow m-2", style:"width: 150px;" if @user.avatar.attached?%>
      <div class="ms-5">
        <p><%= @user.description %></p>
        <%= link_to "Edit my profile", edit_user_registration_path(@user), class: "btn btn-primary" %>
      </div>
    </div>
  </div>
  <div>
    <ul class="nav nav-tabs" id="myTab" role="tablist">
      <li class="nav-item" role="presentation">
        <button class="nav-link active" id="booking-tab" data-bs-toggle="tab" data-bs-target="#booking-tab-pane" type="button" role="tab" aria-controls="home-tab-pane" aria-selected="true">My Bookings</button>
      </li>
      <li class="nav-item" role="presentation">
        <button class="nav-link" id="lesson-tab" data-bs-toggle="tab" data-bs-target="#lesson-tab-pane" type="button" role="tab" aria-controls="lesson-tab-pane" aria-selected="false">My Lessons</button>
      </li>
    </ul>
    <div class="tab-content" id="myTabContent">
      <div class="tab-pane fade show active" id="booking-tab-pane" role="tabpanel" aria-labelledby="booking-tab" tabindex="0"><%= render 'mybookings', teacher_bookings: @teacher_bookings, student_bookings: @student_bookings %></div>
      <div class="tab-pane fade" id="lesson-tab-pane" role="tabpanel" aria-labelledby="lesson-tab" tabindex="0"><%= render 'mylessons', lesson: @mylessons %></div>
    </div>
  </div>
</div>
